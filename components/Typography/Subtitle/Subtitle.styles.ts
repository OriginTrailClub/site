import { css } from 'stitches.config';

export const container = css({
    fontFamily: '$header',
    lineHeight: '$header',
    fontSize: '$subtitle',
    fontWeight: '$medium',
    color: '$text-emphasis',
    m: 0
})