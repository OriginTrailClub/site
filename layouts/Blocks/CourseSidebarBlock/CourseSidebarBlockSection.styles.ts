import { css } from 'stitches.config';

export const lessons = css({
  display: 'flex',
  flexDirection: 'column',

  width: '100%',
  p: '$none',
  m: '$none',

  listStyle: 'none',

  mt: '-$small',
});
