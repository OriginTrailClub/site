import { css } from 'stitches.config';

export const container = css({
    fontFamily: '$mono',
    fontSize: '$body-default',
    lineHeight: '$header',
    color: '$text-header',
    fontWeight: '$bold',
});