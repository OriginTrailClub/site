import { css } from 'stitches.config';

export const container = css({
  fontFamily: '$mono',
  fontSize: '$cell',
  lineHeight: '$body',
  color: '$text-subtle',
  fontWeight: '$regular',
  mt: '$x-small',
});
