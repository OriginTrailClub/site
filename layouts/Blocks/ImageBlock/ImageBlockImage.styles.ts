import { css } from 'stitches.config';

export const container = css({
  display: 'none',
  maxWidth: 1234,
  width: '100%',
  position: 'relative',
});

export const contents = css({
    width: '100%',
    height: 0,
})