import { css } from 'stitches.config';

export const container = css({
  display: 'flex',
  flexDirection: 'column',
  height: '100%',
  borderWidth: 1,
  borderColor: '$gray-300',
  borderStyle: 'solid',
});
