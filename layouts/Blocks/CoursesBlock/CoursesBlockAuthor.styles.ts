import { css } from 'stitches.config';

export const container = css({
  display: 'flex',
  height: '100%',
  alignItems: 'center',
  fontFamily: '$mono',
  fontSize: '$body-compact',
  lineHeight: '$body',
  color: '$text-header',
});
