import { css } from 'stitches.config';

export const container = css({
  gridArea: 'title',
  display: 'flex',
  alignItems: 'center',
  fontFamily: '$header',
  lineHeight: '$header',
  color: '$text-header',
  m: 0,
  fontWeight: '$semi-bold',
  fontSize: '$header-3',
});
